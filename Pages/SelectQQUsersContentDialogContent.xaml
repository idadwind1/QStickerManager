<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="QStickerManager.Pages.SelectQQUsersContentDialogContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QStickerManager.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="8">
        <TextBlock x:Uid="SelectQQUsersInstruction" TextWrapping="Wrap" />
        <ListView ItemsSource="{x:Bind Options.Keys}"
                  Name="ListView"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <CheckBox
                        Content="{x:Bind}"
                        Checked="CheckBox_Checked"
                        Unchecked="CheckBox_Checked" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
