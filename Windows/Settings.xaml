<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="QStickerManager.Windows.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QStickerManager.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <ScrollViewer>
        <StackPanel
            MaxWidth="760"
            Padding="32"
            Spacing="24"
            HorizontalAlignment="Center">

            <TextBlock
                x:Uid="SettingsHeading"
                Style="{StaticResource SubtitleTextBlockStyle}" />

            <StackPanel Spacing="8">
                <TextBlock
                    x:Uid="SettingsStorageHeading"
                    Style="{StaticResource BodyStrongTextBlockStyle}" />

                <Border
                    Padding="16"
                    CornerRadius="8"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <Grid
                        ColumnDefinitions="Auto,*,Auto,Auto"
                        RowDefinitions="Auto,Auto">
                        <TextBlock
                            x:Uid="SettingsLibraryLocation"
                            Grid.Column="0"
                            FontSize="16" />
                        <TextBlock
                            x:Uid="SettingsLibraryDescription"
                            Grid.Column="0"
                            Grid.Row="1"
                            TextWrapping="Wrap"
                            Opacity="0.7"
                            Margin="0,4,16,0" />
                        <Button
                            x:Uid="SettingsOpenFolderButton"
                            Grid.Column="1"
                            Grid.RowSpan="2"
                            Click="OpenStickerFolder_Click"
                            VerticalAlignment="Center" />
                        <Button
                            x:Uid="SettingsChangeLocationButton"
                            Grid.Column="2"
                            Grid.RowSpan="2"
                            Click="ChangeStickerLocation_Click"
                            VerticalAlignment="Center"
                            Margin="8,0,0,0" />
                    </Grid>
                </Border>

                <TextBox
                    x:Uid="SettingsPathTextBox"
                    x:Name="BasePathText"
                    Text="{x:Bind LibraryDirectory}"
                    IsReadOnly="True"
                    IsHitTestVisible="False"
                    TextWrapping="Wrap" />

                <Button
                    x:Uid="SettingsResetButton"
                    Click="ResetSettings_Click"
                    HorizontalAlignment="Left" />
            </StackPanel>

            <StackPanel Spacing="8">
                <TextBlock
                    x:Uid="SettingsMaintenanceHeading"
                    Style="{StaticResource BodyStrongTextBlockStyle}" />

                <Border
                    Padding="16"
                    CornerRadius="8"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <Grid ColumnDefinitions="*,Auto">
                        <StackPanel
                            Grid.Column="0"
                            Spacing="4">
                            <TextBlock
                                Grid.Column="0"
                                x:Uid="SettingsClearCacheHeading"
                                FontSize="16" />
                            <TextBlock
                                Grid.Column="0"
                                x:Uid="SettingsClearCacheDescription"
                                TextWrapping="Wrap"
                                Opacity="0.7" />
                        </StackPanel>
                        <Button
                            x:Uid="SettingsClearCacheButton"
                            Grid.Column="1"
                            Click="ClearCache_Click"
                            VerticalAlignment="Center"
                            Margin="16,0,0,0" />
                    </Grid>
                </Border>

                <Border
                    Padding="16"
                    CornerRadius="8"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <Grid ColumnDefinitions="*,Auto">
                        <StackPanel
                            Grid.Column="0"
                            Spacing="4">
                            <TextBlock
                                x:Uid="SettingsShuffleHeading"
                                FontSize="16" />
                            <TextBlock
                                x:Uid="SettingsShuffleDescription"
                                TextWrapping="Wrap"
                                Opacity="0.7" />
                        </StackPanel>
                        <Button
                            x:Uid="SettingsShuffleButton"
                            Grid.Column="1"
                            Click="ShuffleStickers_Click"
                            VerticalAlignment="Center"
                            Margin="16,0,0,0" />
                    </Grid>
                </Border>
            </StackPanel>

            <StackPanel Spacing="8">
                <TextBlock
                    x:Uid="SettingsAboutHeading"
                    Style="{StaticResource BodyStrongTextBlockStyle}" />

                <Border
                    Padding="16"
                    CornerRadius="8"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <Grid ColumnDefinitions="Auto,*">
                        <StackPanel
                            Grid.Column="0"
                            Spacing="8">
                        <TextBlock
                            Text="QStickerManager"
                            FontSize="16" />
                        <TextBlock
                            Text="{x:Bind VersionText}" />
                        <HyperlinkButton
                            Content="GitHub"
                            NavigateUri="https://github.com/idadwind1/QStickerManager"
                            HorizontalAlignment="Left" />
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Window>
